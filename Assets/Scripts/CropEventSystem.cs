using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CropEventSystem : MonoBehaviour
{
    // variable which can read the bots crops that you are currently interacting with...
    public int currentBotsCrops = 0;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
